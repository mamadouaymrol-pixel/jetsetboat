Fix two issues:

1. MENTIONS LEGALES page - fix spacing:
In mentions-legales.astro reduce the huge gaps between sections.
Max margin between sections: 2rem.
Add a light border-bottom on each section for separation.
The page should feel compact and readable like a document.

2. CONTACT background image - it's still not visible enough:
In Contact.astro change the ::before pseudo-element:
- opacity: 0.22 → increase to 0.35
- Add a very subtle overlay on top so text stays readable:
  after the ::before, add a ::after with 
  background: rgba(255,250,245,0.45) to soften the image
  without hiding it completely
- The image contact-bg.jpg should be clearly visible 
  as a real background, not just a hint